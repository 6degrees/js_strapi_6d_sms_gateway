[phases.install]
cmds = ['cd src', 'pnpm install'] # Changed to pnpm as per your preference

[phases.build]
cmds = ['cd src', 'pnpm build'] # Changed to pnpm as per your preference

[variables]
NODE_ENV = 'production' # Setting the environment to production
NIXPACK_NODE_VERSION = '20' # Specifying Node.js version

[start]
cmds = ['cd src', 'pnpm start'] # Changed to pnpm as per your preference