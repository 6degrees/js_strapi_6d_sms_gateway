version: "3"

services:
  app:
    build:
      context: ./app
    container_name: strapi_6d_sms
    restart: unless-stopped
    env_file: app/.env
    volumes:
      - ./app:/srv/app
    ports:
      - 127.0.0.1:8055:1337
