providers = ["node"]

[phases.install]
cmds = ['npm run install'] # Changed to pnpm as per your preference

[phases.build]
cmds = ['npm run build'] # Changed to pnpm

[variables]
NODE_ENV = 'production' # Setting the environment to production
NIXPACK_NODE_VERSION = '16' # Specifying Node.js version

[start]
cmd = 'npm run start' # Changed to pnpm
